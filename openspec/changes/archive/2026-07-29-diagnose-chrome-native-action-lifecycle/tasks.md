> **Completion status (2026-07-30):** The Owner explicitly accepted cumulative PR #76 at immutable exact `b8f5a4a8d4c001a4963be706dab7c6891efe75c5` and authorized merge. Exact CI run `30489904228` passed setup/linter/tests/build 4/4, fresh Review task #362 passed P0/P1/P2 `0/0/0`, and PR #76 merged into `develop` through `88a8af17e9560dc15a36e29412d3df52ef69a220`. A checked item means implemented, superseded by a later accepted exact, or explicitly closed by Owner acceptance; it does not reinterpret a historical BLOCKED, FAIL, UNVERIFIED, or unexecuted browser result as PASS. QA task #363 remained nonblocking at merge. Its later post-merge report recorded the Chrome MV3 unreachable-endpoint failure path as PASS, the Chrome success path as UNVERIFIED, and Firefox MV2 as UNVERIFIED; report SHA-256 `f840338225a53d16f863775721bd2e4ef7fcd7059dedc5e2126ac997dd995136` and log SHA-256 `c3fc31fcbb3a8950126aefde559e82e7c13ca345af62cdd4a1a48df293bcb95b`.

## 1. Authority And Evidence Reset

- [x] 1.1 Bind the executable-precondition docs authority to package-first parent `74d0c67eaf24f68bf731c8c9205cea7aa6c6792c` (tree `6fd9693794491d5ec6c59084925ec8a2d78bd521`) and record Round 18 `3ae2b81ebaf31dfd368affabdd387fe204929420` plus every earlier blocked candidate as neither parent nor transferable evidence.
- [x] 1.2 Record QA task #309 as a Chrome executable verification-control blocker, not a product/package/helper verdict. Its automated, static, Review, Chrome, cleanup, and partial results do not transfer, and Firefox was not reached.
- [x] 1.3 Record task #310's read-only causal result: the temporary adapter hardcoded branded Chrome 150 instead of receiving the repository canonical's explicit side-load-capable Chrome for Testing/Chromium executable.
- [x] 1.4 Record the preserved facts: package MV3 declares `background.service_worker = background.js`; the artifact contains that entry; CDP discovery classified the foreign Google Network Speech worker; no WebChat target/session appeared; and no helper timing/source repair is supported.
- [x] 1.5 Freeze the direct tracked implementation whitelist: new `e2e/chrome-native-action-lifecycle.ts`, new `e2e/chrome-native-action-lifecycle.test.ts`, and checkbox-only updates to this file. Treat every other tracked path as protected.
- [x] 1.6 Freeze the two source byte identities from Round 18: helper SHA-256 `6763e476722dd7b7675820b753806ea5908e13560eeef328a15d6ce4c27ff160` and focused-test SHA-256 `9f4a27172bf0f60446dd5f1c5776d14df95dc4c295e1c49c0e9d019eb7f2baa5`. Any byte change requires new authority.
- [x] 1.7 Freeze the real adapter as a test-owned, SHA-256-recorded QA artifact outside tracked repository scope. It may not change helper/product/package/lifecycle semantics.
- [x] 1.8 Bind this superseding docs authority as a clean detached/ref-free sole child of executable docs exact `e63d9cc65f8cd8ec48ab400d7f023b7dbb708143` (tree `7cccf460a5e71add491fd244c4b3a963800b3f84`), with `74d0c67eaf24f68bf731c8c9205cea7aa6c6792c` as its sole grandparent. Round 24 `2863c2d60d72940df6ccadc0cdbbaec6c1657352` is not a parent.
- [x] 1.9 Record QA task #324 as FINAL BLOCKED on an invalid external-artifact format invocation: oxfmt ran from the artifacts cwd, reported `No config found, using defaults`, and stopped before browser work. Report SHA-256 is `8d1d3a29687829106d429d79cbea0cc24327c60c0aba61953df2e1652b4d9eea`; no partial PASS or format verdict transfers.
- [x] 1.10 Freeze the candidate formatter policy identities: `oxfmt 0.57.0`; `.oxfmtrc.json` blob `de8b360dbe361cbddd5e6e3ed96342c2fbb3610c` and SHA-256 `d33493ebef365f935cb5e05baf60bda7d0e52d2602cafa2310f0da35d4e2fe91`; `.gitignore` blob `78a73c36c91468155b87ee16495c91900d672c10` and SHA-256 `969b475b5c846c9a6ad8a1f466ca930dd68f5f8e91733aab104c186c53826ead`.
- [x] 1.11 Record that QA task #324 proves no artifact-format defect and authorizes no adapter byte change, tracked config/dependency/script edit, browser rerun, or evidence transfer.

## 2. Exact External Artifact Format Precondition

- [x] 2.1 Resolve one immutable candidate root and its candidate-installed `oxfmt 0.57.0` after frozen-lockfile install. Reject a global binary, `npx`/`pnpx` download, wrong version, PATH fallback, Owner checkout, artifact directory, ancestor, home directory, or current cwd as formatter authority.
- [x] 2.2 Require the protected candidate-root `.oxfmtrc.json` and `.gitignore` at the recorded blob/SHA-256 identities. Supply both paths explicitly; reject missing, changed, unreadable, unparseable, defaulted, discovered, or nested policy.
- [x] 2.3 Run one read-only `--check` with explicit `--config`, `--ignore-path`, and `--disable-nested-config`, naming exactly the frozen regular-file `chrome-canonical.mjs` and `chrome-adapter-controls.test.mjs` inputs. Reject positional `.`, directories, globs, exclusions, duplicate/extra/non-regular/symlinked inputs, unmatched suppression, and `--write`.
- [x] 2.4 Record candidate exact/tree, formatter version, repo-relative config/ignore paths plus blob/SHA identities, sanitized invocation shape, both input basenames/SHA-256 values, exit/result, and zero-side-effect state. Keep raw absolute paths transient and out of reports/helper/chat.
- [x] 2.5 Emit `artifact-format-precondition-failed` for any formatter/config/ignore/input/invocation contradiction, including `No config found`; make no artifact mismatch claim and prove adapter controls, helper, executable precondition, profile, CDP, browser, process, port, target, and native action did not begin.
- [x] 2.6 Emit `artifact-format-failed` only after the complete exact policy and both input identities are proven and configured `--check` reports a mismatch. Stop without format write, repair, config fallback, or rerun. If PASS, continue without consuming or resetting the existing 10-second or 30-second budgets.
- [x] 2.7 Add external fixture controls proving identical configured results from candidate-root and artifact-root cwd, default/global/nested config rejection, config/version/hash/parse failures, exact two-file boundaries, read-only operation, and distinct configured-mismatch semantics without touching canonical artifacts.
- [x] 2.8 Freeze fresh real-adapter, controls, and report SHA-256 identities before Review. If the first correct implementation check proves a canonical artifact needs modification, stop for a separate artifact-format repair authority rather than changing it under this invocation authority.

## 3. Explicit Chrome Executable Precondition

- [x] 3.1 Require one caller-injected absolute executable path. Reject a missing or relative value, branded application hardcode, default, `PATH` lookup, application scan, browser discovery, fallback, or Owner-browser reuse.
- [x] 3.2 Before any persistent profile, CDP browser, helper budget, or helper call, use one non-resetting maximum 10-second precondition to resolve `realpath`, require a regular executable with execute access, hash the requested path/canonical path/executable bytes, and run exactly one bounded `--version` probe.
- [x] 3.3 Accept only an exact `Google Chrome for Testing <version>` or `Chromium <version>` identity with a non-empty version. Reject branded Google Chrome, Microsoft Edge, derivatives, unknown/ambiguous labels, symlink-to-rejected targets, non-zero probe exit, timeout, output over 512 bytes, or probe/launch canonical-path mismatch.
- [x] 3.4 Record only product family, parsed version, requested-path SHA-256, canonical-path SHA-256, executable-byte SHA-256, bounded version-output SHA-256, and one derived privacy-safe executable identity token. Keep raw absolute paths transient and out of reports/helper evidence.
- [x] 3.5 Emit distinct `chrome-executable-precondition-failed` evidence before lifecycle on any invalid or unresolved identity. Prove the bounded probe is cleaned and no persistent profile, CDP, worker discovery, accepted target, helper timeline, or native action began; do not map it to a helper outcome.
- [x] 3.6 Cover accepted Chrome for Testing and Chromium plus missing, relative, nonexistent, non-executable, branded Chrome, branded Edge, derivative, unknown, ambiguous, symlink-to-rejected, non-zero, timeout, output-overflow, and probe/launch-mismatch controls without launching canonical lifecycle.
- [x] 3.7 After PASS, launch exactly the validated canonical executable with the exact package, isolated profile, and only `about:blank`, pass the privacy-safe identity token into the unchanged helper context, and provide no alternate executable or retry.

## 4. Deterministic Lifecycle Diagnostic

- [x] 4.1 Recreate the dependency-free helper and focused test byte-identically to the recorded Round 18 SHA-256 values. Do not add executable logic, a package script, existing-runner edit, reporter, aggregator, or general cleanup owner.
- [x] 4.2 Require `about:blank` startup; establish target discovery, flattened auto-attach, per-session Runtime/Log/Page observation, and the event sink before accepted-target creation.
- [x] 4.3 Parse the packaged manifest first and require MV3 plus a non-empty `background.service_worker`. Do not accept a caller-supplied extension ID or a first/global-only worker shortcut.
- [x] 4.4 Use key-order-only canonical JSON and one non-resetting maximum 30-second pre-target discovery deadline. Classify every current worker before deciding uniqueness; derive the extension ID only from one URL-host/Runtime-ID, exact-entry, canonical-manifest match.
- [x] 4.5 Record bounded privacy-safe per-worker order/time, target URL/session, Runtime ID, allowlisted manifest projection, packaged/Runtime SHA-256 canonical digests, and capped sorted JSON Pointer diff evidence. A diff-overflow marker remains non-equal by digest; fail on total worker-record overflow or unresolved classification.
- [x] 4.6 Treat zero exact candidates at the deadline or more than one at a decision fence as `extension-setup-failed`. Keep classified foreign workers as evidence only; never let them supply identity or replace the exact binding.
- [x] 4.7 Bind the exact worker target/session/ID/entry/manifest tuple immutably. Classify later workers only within the remaining current budget; reject unresolved, exact duplicate/replacement, or tuple change while allowing a fully classified unrelated worker to remain evidence only.
- [x] 4.8 Issue exactly one `Target.createTarget({ url: 'https://example.com/' })`; bind its target/session/main frame; reject startup accepted pages, second targets, replacement, `Page.navigate`, reload, refresh, retry, and repair.
- [x] 4.9 Use one non-resetting maximum 30-second accepted-target lifecycle budget and one exact binding across navigation, isolated context, logs, DOM, terminal outcome, and action authorization.
- [x] 4.10 Record a bounded ordered timeline for worker discovery/continuity, target/frame lifecycle, page-bound execution contexts, normalized console/exception events, structural DOM samples, and an unconditional final state. Capture no arbitrary DOM, manifest raw values outside the safe projection, cookies, storage, credentials, or user data.
- [x] 4.11 Emit exactly one fail-closed helper outcome: `extension-setup-failed`, `target-lifecycle-failed`, `content-context-absent`, `shared-runtime-unavailable`, `content-mount-absent`, `unexpected-content-failure`, or `mounted`.
- [x] 4.12 Return action authorization only for clean `mounted` with the same worker and page binding; never click, dispatch an action event, call the product command, open options, or repair a failed lifecycle from the helper.

## 5. Focused Controls And Protected Scope

- [x] 5.1 Cover the exact formatter/version/config/ignore/input policy and fail-closed format outcomes, then cover invalid packaged MV3/background entry, foreign-first/exact-late, only-foreign, duplicate exact, unresolved candidate, entry mismatch, Runtime-ID mismatch, canonical manifest mismatch, accepted object-key reorder, and rejected array/value/field/default/alias/path differences.
- [x] 5.2 Cover a fully classified unrelated worker after binding, exact duplicate/replacement, and every bound worker target/session/ID/entry/manifest continuity change.
- [x] 5.3 Cover correct observation-before-target chronology and reject an accepted startup page, late listeners, a second target request, target replacement, wrong session/frame, foreign context, deadline reset, retry, reload, and repair.
- [x] 5.4 Cover bounded diff-overflow evidence, total worker/event overflow, worker/options/main-world contexts, `Shared runtime unavailable`, another extension error, isolated-context-without-mount, false host-page root, missing final evidence, and clean mount authorization.
- [x] 5.5 Prove the helper never invokes native action and that every non-mounted or invalidated-binding branch withholds the caller's click callback before activation.
- [x] 5.6 Prove both tracked source files match the recorded SHA-256 values and every existing E2E file, including Chrome harness/runtime and both Firefox precondition files, remains byte-identical.
- [x] 5.7 Prove `.oxfmtrc.json`, `.gitignore`, `package.json`, `pnpm-lock.yaml`, `src/**`, WXT/manifest/permissions, dependencies, package scripts, timeouts, Runtime/coordinator/Offscreen/protocol/storage/UI, workflows/CI, tracked reports, release metadata, refs/remotes, and Owner checkout remain unchanged.
- [x] 5.8 Run implementation-owned exact-configured artifact format, adapter controls, focused/full tests, tracked format/lint/type, strict OpenSpec, production Chrome MV3 and Firefox MV2 build/package/manifest, scope, and lineage gates. Do not run or claim canonical browser QA from the implementation seat.
- [x] 5.9 Freeze one clean detached/ref-free immutable implementation exact as the sole child of this docs exact and freeze the test-owned real-adapter, controls, and report SHA-256 values. Do not push, update PR/CI, merge, release, or touch the Owner checkout.

## 6. Fresh Review And Full Cross-Browser QA

- [x] 6.1 Obtain one fresh Reviewer PASS for candidate lineage, exact formatter/config/ignore policy, cwd-independent read-only invocation and controls, adapter artifact identities, executable precondition, the two tracked source digests, package-first identity authority, strict canonical relation, worker evidence/cardinality/continuity, chronology, target/context binding, terminal precedence, action withholding, direct scope, and protected surfaces.
- [x] 6.2 After Review PASS, obtain one fresh same-seat QA verdict from zero for every required gate and the complete real Chrome MV3 plus Firefox MV2 matrix. Do not reuse QA task #287, #298, #309, #324, or any earlier candidate evidence.
- [x] 6.3 After identity and frozen-lockfile install, run the exact-configured external artifact format precondition/check before browser resources. Stop at the first precondition or configured mismatch without fallback, write, repair, rerun, or browser startup.
- [x] 6.4 In Chrome, record the explicit accepted CfT/Chromium identity and adapter SHA-256, pass executable precondition before lifecycle, start only `about:blank`, preserve every worker record/diff, derive one exact worker/manifest binding, create the accepted target exactly once, preserve the full timeline, and reach clean `mounted` without fallback, retry, reload, target substitution, or unexpected error.
- [x] 6.5 After Chrome `mounted` authorization, perform exactly one real native toolbar action, prove `afterOptionsCount - beforeOptionsCount = 1` plus pre/post-action content Runtime on the same accepted target, and reject event dispatch, direct product invocation, manual options navigation, or post-click repair.
- [x] 6.6 In Firefox, rerun initial startup plus two same-profile owned-process restarts and satisfy every existing exact add-on, Background Page, physical identity, one native click, options `+1`, pre/post Runtime, unexpected-error, and no-repair requirement in every generation.
- [x] 6.7 Prove zero residual formatter temp/output, executable probes, owned Chrome/Firefox browsers, drivers, profiles, packages, processes, ports, sessions, listeners, tabs, and temporary resources without touching unrelated Owner resources.

## 7. Follow-Up Routing

- [x] 7.1 If artifact format precondition fails, stop as a verification setup result; do not use default/global/nested policy, modify config/dependencies/scripts/artifacts, rerun QA task #324, or start browsers.
- [x] 7.2 If the first correctly configured implementation or QA check emits `artifact-format-failed`, preserve the read-only evidence and route separate narrow artifact-format repair authority; do not write or reformat under this invocation authority.
- [x] 7.3 If executable precondition fails, stop as a harness setup result; do not invoke the helper, select another executable, retry task #309, or edit product/package/helper source.
- [x] 7.4 If an accepted explicit executable still reaches `extension-setup-failed` because no exact worker appears, route a separate activation/observation authority from that fresh artifact; do not reinterpret the branded-Chrome run.
- [x] 7.5 If the helper ends `content-context-absent`, `shared-runtime-unavailable`, `content-mount-absent`, or `unexpected-content-failure`, route the corresponding separate product/browser-lifecycle authority without broadening this change.
- [x] 7.6 If the full matrix passes, retain exact-bound artifacts for later release coordination, but do not push, mutate PR/CI, merge, release, or update the Owner checkout without separate explicit authorization.
