## 1. Product Authority

- [x] 1.1 Define one Content composition owner for terminal exit, BFCache suspension, BFCache restoration, page lease ownership, and page-scoped Runtime feedback.
- [x] 1.2 Require feedback silence before cleanup, exact one-time restoration, current Runtime reconciliation, terminal finality, and repeated-cycle idempotency.
- [x] 1.3 Keep shared Runtime/Room, protocol, persistence, retry, permissions, dependencies, and public UI structure, controls, and copy unchanged.
- [x] 1.4 Freeze the six reported lifecycle regressions plus current-not-ready and shared-Runtime isolation controls.

## 2. Deterministic Failure Controls

- [ ] 2.1 Add a parent FAIL-before that composes document lifecycle, ClientLease release, readiness mapping, and AppFeedback to prove terminal cleanup creates the connection loading entry.
- [ ] 2.2 Add a parent FAIL-before that proves a persisted A -> B -> Back restoration reuses the suspended document without restarting its page binding and leaves page feedback stale while the shared Runtime is ready.

## 3. Minimum Repair

- [ ] 3.1 Replace independent unload ownership with one BFCache-aware Content document lifecycle owner.
- [ ] 3.2 Stop and remove page-scoped readiness feedback before canceling page work and releasing its lease exactly once.
- [ ] 3.3 Restore one persisted document binding exactly once, consume the current Runtime snapshot, and ignore duplicate signals and late results.
- [ ] 3.4 Preserve terminal cleanup, ordinary visibility behavior, shared Runtime/Room ownership, protocol, persistence, retry semantics, and all unrelated feedback.

## 4. Verification And Delivery

- [ ] 4.1 Pass candidate controls for terminal exit, hard refresh, BFCache hide, BFCache show with Runtime ready, restored current-not-ready behavior, repeated history cycles, and shared Runtime isolation.
- [ ] 4.2 Pass focused and full Vitest, TypeScript, format, lint, strict OpenSpec/status/doctor, React Doctor, Chrome/Firefox production builds, scope, identity, current-only, and clean-worktree gates on one immutable exact.
- [ ] 4.3 Obtain a fresh architecture-first Inspector verdict on that same exact; implementation authorship and fresh Review SHALL remain independent, and the user-visible repair SHALL use a `fix` conventional commit.
- [ ] 4.4 Execute and record non-blocking real desktop browser checks for A -> B -> Back and refresh/navigation. Report an environment limitation as BLOCKED or unverified, never as PASS; do not route QA, QC, or UX unless the Owner explicitly requests them.
- [ ] 4.5 Publish this requirement through one feature branch and Draft PR targeting `develop`; after Owner acceptance, let PM close only final OpenSpec/task truth before any authorized Ready/merge flow.
