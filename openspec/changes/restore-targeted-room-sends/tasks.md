## 1. Freeze Targeting And Timing Authority

- [x] 1.1 Bind the authority to current `develop@83719009ab88e909ec8e4bb7d14b70cb693e31ea` and record the reproduced local send-side connecting-DataChannel failure without inferring remote peer state.
- [x] 1.2 Inventory every current product omitted-target producer: initial Session publication, Text/Reaction, History inventory-request page, World full publication, and World publication retry.
- [x] 1.3 Freeze the low-level optional-target API and exact `undefined|string|string[]|[]` meanings while requiring every current room-wide product producer to use one explicit logical-recipient array or zero provider calls for an empty result.
- [x] 1.4 Freeze producer-owned current recipient sources, deterministic de-duplication/self exclusion without a separate grace/departure classifier, existing targeted catch-up/response behavior, one array provider call, and Artico's original first-throw interruption.
- [x] 1.5 Freeze only the direct Domain join, World recovery/replacement join, and join-resumed never-invoked head continuations as `join -> sleep(1000) -> Room.send`; affected room-wide producers derive/filter/de-duplicate/exclude-self current peerIds only after sleep, while explicit targeted heads retain their recipient meaning without re-filtering. Freeze no pre-sleep room-wide target snapshot, second chain delay, room-wide gate, shared queue, later-flow delay, or new user-send delay.
- [x] 1.6 Freeze cancellation, supersession, teardown, unchanged timeout, no-late-timer, no provider-call retry/outbox/classifier, unchanged zero-call World release preflight retry, local projection/persistence, History, protocol, dependency, PR #135, and Draft-only boundaries.
- [x] 1.7 Synchronize every live conflicting non-archive target/attempt-all statement in `standardize-functional-iteration`, `absorb-transient-recovery-without-error-toasts`, and canonical provider/per-target authority.

## 2. Add Fail-Before Controls

- [x] 2.1 Add a complete producer inventory control that fails while any of the five product paths reaches `Room.send` with an omitted target or while an eligible current producer is absent from the inventory.
- [x] 2.2 Add mutation-sensitive Text/Reaction, initial Session, History inventory, World full, and World retry controls for exact current-owner target source, first-seen de-duplication, self exclusion without a separate grace/departure classifier, one non-empty array call, and zero provider calls for empty recipients.
- [x] 2.3 Prove the pinned Artico array call preserves the original first target Error and interrupts a later sendable target; fail if WebChat expands targets, continues attempt-all, retries after provider invocation, or classifies the Error text. Preserve the existing World release retry only when preflight made zero provider calls.
- [x] 2.4 Prove each direct join-followup chain makes no target snapshot or provider call at 999ms, re-checks ownership and derives then-current membership targets at 1000ms, reflects joins/leaves during the wait, sleeps only once for Domain Session -> World, and leaves explicit-target meaning plus user/later automatic/control timing unchanged.
- [x] 2.5 Prove leave, teardown, cancellation, join/recovery supersession, and Runtime replacement during sleep make the old continuation inert with no provider call, state commit, failure, retry, or successor mutation.
- [x] 2.6 Assert the restored v1.9.7 explanation and both exact Artico source references while prohibiting `UserList`, `readyPeers`, DataChannel inspection, or a replacement target-attempt abstraction.

## 3. Implement The Closed Repair

- [x] 3.1 Resolve each named producer's targets from its existing Session, History, World, or current-generation membership owner and pass exactly one non-empty array to the unchanged private transport API; settle empty recipients without invoking the provider.
- [x] 3.2 Preserve every existing explicit single/array catch-up and response recipient, History request identity and provider lanes, World revision ownership, local Chat acceptance/persistence/display, and the original provider Error route.
- [x] 3.3 Replace no array with per-target iteration, attempt-all, provider-call retry, outbox, acknowledgement, readiness cache, remote/DataChannel state read, or error-text classification; preserve only the existing zero-call World release preflight retry and keep dependencies and lockfile unchanged.
- [x] 3.4 Add only call-site-local one-second sleeps to the exact direct join continuations; after sleep, re-check their current owner, then either derive/filter/de-duplicate/self-exclude current peer ids for a room-wide producer or preserve an already explicit target before sending. Add no pre-sleep room-wide target snapshot, explicit-target re-filter, room-wide generation gate, shared delayed-send queue, persistent timer, or later-flow delay.
- [x] 3.5 Restore the v1.9.7 explanatory comment and both historical source-reference lines byte-for-byte at the shared product selection boundary, changing only obsolete `UserList`/`SyncUser` wording to current Sessions/application-layer membership wording.

## 4. Verify And Review

- [x] 4.1 Run focused and full Vitest, TypeScript, Oxfmt/Oxlint, Chrome/Firefox production builds, strict focused/full OpenSpec, status, doctor, authority-residue, scope, identity, and repository-cleanliness gates.
- [x] 4.2 Prove the cumulative source diff changes no protocol/schema/persistence/History/local-projection/UI/dependency/lock behavior outside the exact targeting and direct-join timing authority, and keeps PR #135 unchanged.
- [x] 4.3 Freeze one immutable source exact with sole-parent/local-remote-PR identity and terminal hosted CI, then obtain one fresh cumulative Inspector review of the exact and all mutation-sensitive controls.
- [x] 4.4 Keep the pull request Draft and do not mark Ready, merge, deploy, release, or publish without later explicit authority.
