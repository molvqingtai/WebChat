## 1. Freeze Source Authority And Controls

- [ ] 1.1 Start from one clean sole child of this reviewed docs authority; record the exact, tree, parent, source/protocol/public-port identities, and current focused/full gate baselines before source edits.
- [ ] 1.2 Add a deterministic fail-before integration control with healthy transport and four logical room members where one retained `ended` remote observation makes the released AppButton-equivalent reconnect remain at three while complete release/reopen reaches four.
- [ ] 1.3 Add candidate assertions requiring one manual Refresh to accept that still-online member's valid same-presence SESSION and converge to four without closing a tab or waiting for last-page grace.
- [ ] 1.4 Add controls covering every complete-destruction family: physical Chat owner/peer and trusted room; Connection attempts/generation/readiness; Session and observers; pending leaves; History requester/provider/supply/bindings/feedback; volatile Delivery/batches; snapshots/baselines; send/decode queues; and all domain-scoped timers, callbacks, operations, caches, and fences.
- [ ] 1.5 Audit initial join, reconnect, automatic recovery, same-domain page attach/reattach, grace return, and host recovery for any path that can accept an existing `presenceId` while retaining an `ended` observation; add a focused regression or documented same-live-connection proof for each path.
- [ ] 1.6 Add fail-before and protected controls proving the common ended guard rejects a legitimate current trusted replacement today, while exact old-`sessionId` replay, superseded room/source/attempt work, identity/time mutation, and newer-generation conflict remain rejected.
- [ ] 1.7 Add controls proving one ready-state AppButton activation starts both physical replacements, the old World owner leaves before its replacement joins, active World registrations survive, the projected list rebuilds only from the new generation, and pre-ready Retry starts no manual World replacement.

## 2. Implement Complete Domain And World Connection Destruction

- [ ] 2.1 Add one generation-fenced destruction orchestration across the existing transport/Wire, Connection, Session, History, and Delivery owners; it must remove the complete current-domain connection aggregate before replacement preparation or authoritative inbound processing begins.
- [ ] 2.2 Retain only the active local logical `{presenceId, joinedAt, user, site}` seed, rotate physical `peerId` and `sessionId`, and prevent old callbacks, timers, cleanup, or terminals from affecting the replacement.
- [ ] 2.3 Sequence the existing scoped transport join, SESSION publication/validation, attempt-owned inbound processing, fresh History/Delivery work, and canonical commit after destruction settlement; add no tombstone exception, member-fetch/replay path, fallback, migration, or compatibility route.
- [ ] 2.4 Preserve current page leases and same-domain subscriptions so all attached pages converge to one clean replacement snapshot without final release, last-page grace, durable logical retirement, or stale snapshot replay.
- [ ] 2.5 Make one accepted ready-state AppButton action start the current-Domain and World children without serializing their starts; retain the existing Domain reconnect request as the sole AppButton availability, disabled/loading, completion, and error owner, and leave pre-ready Retry unchanged.
- [ ] 2.6 Add one independently fenced World reset that removes the prior physical owner/peer, trusted membership, room members, remote projection, connection/recovery generation, pending connection-scoped publication, queues, timers, callbacks, and stale completions while preserving committed active registrations, user/site values, desired demand, and the current complete local presence.
- [ ] 2.7 Settle the old World physical leave/disposal before canonical rejoin, then publish exactly one current full World snapshot through the fresh generation; do not remove a Domain registration, publish an artificial empty-registry snapshot, or accept old-generation work.
- [ ] 2.8 Coalesce the manual World child with current automatic recovery or a prior manual replacement through one in-flight World operation; make Domain and World success/failure/cancellation independent, restore the AppButton on Domain settlement without waiting for World, and project no World manual-refresh state or error into UI/Toast.

## 3. Prove Preservation And Isolation

- [ ] 3.1 Prove Refresh preserves persistent messages, configured identity/settings, current page and page-owned state, local logical `presenceId`/`joinedAt`, and absence of a false logical leave/join notice.
- [ ] 3.2 Prove the current page lease remains attached, prior Domain/World readiness cannot satisfy either replacement, active World registrations and desired sites keep their values, the World physical generation changes, and both replacement connections publish only their current full presence through existing flows.
- [ ] 3.3 Prove every other domain's Chat peer/trusted room, attempts/readiness, sessions/presence, History/Delivery work, messages/member snapshot, timers, queues, and recovery state remain unchanged.
- [ ] 3.4 Prove reset or persistence failure leaves the request retryable and commits no mixed old/new snapshot; delayed old-generation SESSION, leave timer, cleanup, and terminal work has no current authority.

## 4. Repair Common Same-Presence Rebinding

- [ ] 4.1 Replace the unconditional observer-`ended` SESSION rejection with one shared classifier that accepts only a current trusted room/source, a new physical `sessionId`, exact accepted `presenceId`/`user.id`/`joinedAt`, and no newer active conflict.
- [ ] 4.2 On legal correction, replace the old physical binding, mark the same logical observation active, cancel/fence matching pending physical-leave work, converge membership, and start only current History work without allocating a logical generation or duplicate join.
- [ ] 4.3 Apply the classifier consistently to manual reconnect, automatic recovery, page attach/reattach, grace return, and supported host recovery; paths retaining one already-live connection must remain no-op for logical membership.
- [ ] 4.4 Reject exact ended-`sessionId` replay, untrusted/superseded transport work, identity/time mutation, and a binding superseded by a newer logical generation; prove arrival order alone never grants authority.

## 5. Verify External Boundaries And Deliver

- [ ] 5.1 Prove peer wire messages/schemas, codec/version, namespaces, room identifiers, canonical bytes, persistence schemas, dependencies, the exact public `ChatRoom` interface, and the source-free `WorldRoomExtern` interface remain unchanged and existing peers require no upgrade.
- [ ] 5.2 Run focused Runtime/application controls, the canonical full test suite, typecheck, format/lint checks, Chrome/Firefox production builds, and strict OpenSpec/status/doctor gates on the same source exact.
- [ ] 5.3 Freeze one clean immutable source exact with sole parent, tree, patch, exact-bound gate evidence, fail-before provenance, and zero unintended paths; do not merge, deploy, or modify Host/DB/container/production without separate authority.
- [ ] 5.4 Obtain fresh independent Inspector Review of the exact for complete Domain/World state reset, real World stop-before-start replacement, registration/full-snapshot preservation, UI/result independence, overlap coalescing, lifecycle-wide legal rebinding, true stale rejection, logical identity preservation, canonical-path reuse, stale-work fencing, cross-Domain/data protection, and unchanged external contracts.
