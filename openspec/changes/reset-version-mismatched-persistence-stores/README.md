# reset-version-mismatched-persistence-stores

Use one version-neutral canonical message identity and destructively reset only active message/configuration scopes whose own persisted versions are incompatible.
