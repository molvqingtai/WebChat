## 1. Freeze Product And Evidence Authority

- [x] 1.1 Audit the complete current executable inventory as 11 empty/comment-only catch clauses plus 25 no-op Promise rejection handlers, excluding seven non-executable static architecture-HTML catches.
- [x] 1.2 Classify all 36 sites into genuine loss, narrowly provable terminal cleanup, explicit validation, duplicate-rejection observation, unit-test suppression, and E2E cleanup evidence without treating syntax alone as product semantics.
- [x] 1.3 Define one owner for every genuine caught Error: preserve its original rejection, route one structured current-user failure, or call `console.error(error)` directly when structural facts prove no user impact; keep lifecycle control independent of error content and lower layers independent of Toast.
- [x] 1.4 Require every user-impacting UI projection to pass the original `error.message` directly to existing `toast.error` with no prefix, suffix, wrapper, mapping, normalization, or replacement copy; allow user-irrelevant errors to use direct `console.error(error)` instead.
- [x] 1.5 Preserve attempt-all/listener isolation, primary-error precedence, current-user `toast.error`, direct-console unroutable/manual-World/error-delivery/secondary-cleanup failures, and all named non-error outcomes.
- [x] 1.6 Define test/E2E evidence that asserts expected rejection reasons, records every cleanup failure, preserves an earlier primary result, and blocks PASS on cleanup failure.
- [x] 1.7 Freeze the audited routing map: current connection/page-attachment/current-projection failures use the unchanged-message UI path; detached cleanup, install-time preparation, secondary abort/settlement, terminal Port cleanup, manual World, and no-route failures use direct console diagnostics; secondary Promise observers add neither.

## 2. Repair Runtime And Source Ownership

- [x] 2.1 Add only the smallest closed internal routing needed for audited failures that affect a current user; retain event identity, original message, subsystem, operation, and exact scope without public/protocol/database API expansion or Toast imports below the application boundary, and use direct `console.error(error)` for structurally user-irrelevant failures.
- [x] 2.2 Repair the six direct genuine source losses in Artico peer close, PagePort History cancellation, Coordinator reconciliation, and Memory/IndexedDB watcher notification while preserving required stop-before-start, settlement, attempt-all, and listener-isolation behavior.
- [x] 2.3 Narrow IndexedDB abort and PresenceStore Port disconnect to proved already-terminal conditions and route every unexpected cleanup throw without replacing a primary failure.
- [x] 2.4 Repair the three Promise failures with no surviving evidence in Coordinator rebuild attachment, install-time storage preparation, and IndexedDB post-callback settlement.
- [x] 2.5 Rewrite the 16 duplicate-rejection consumers so their original returned/emitted failure remains the sole product owner, their secondary settlement purpose is explicit, and they create neither duplicate diagnostics/Toasts nor unhandled rejection.
- [x] 2.6 Route current-user-impacting failures through the existing Runtime/application boundary by passing the exact original `error.message` directly to `toast.error` without decoration; use direct `console.error(error)` for no-impact/no-route failures and prove the manual AppButton World child remains UI/Toast-silent.

## 3. Repair Unit And E2E Evidence

- [x] 3.1 Replace the five ChatRoom/Recovery/Server test rejection sinks and the ClientLease startup-timeout sink with exact expected-reason and terminal-state assertions that fail on unrelated rejection; replace Recovery's additional no-op error listener with captured and asserted errors without changing the 36-site catch inventory.
- [x] 3.2 Replace the Firefox invalid-URL empty catch with explicit validation that preserves its current failure and accepts no invalid input.
- [x] 3.3 Capture all three synchronous/asynchronous Chrome close failures and every unexpected profile-verification I/O failure with resource/phase/original-message cleanup evidence, continue bounded teardown, preserve any earlier primary result, and fail an otherwise passing run on cleanup failure.
- [x] 3.4 Add mutation-sensitive focused controls for current-user Toast versus direct-console classification, duplicate-owner exactly-once behavior, narrow cleanup and production profile-verification classification, attempt-all and failure-listener continuation, watcher backend parity, primary-plus-cleanup ordering, and manual-World console-only behavior.

## 4. Verify And Deliver

- [x] 4.1 Account for all 36 audited sites in the cumulative diff and prove no empty/comment-only catch, no-op `.catch(() => {})`, generic swallow helper, broad shared-open absorption, broad DevToolsActivePort read/polling fallback, broad profile-verification fallback, or equivalent unowned rejection remains outside an explicitly proved exception; the live startup and cleanup compositions accept only structured `ENOENT` as absence/removal and immediately preserve every other I/O Error with its identity intact.
- [x] 4.2 Run focused and full Vitest, TypeScript, Oxfmt/Oxlint, Chrome/Firefox production builds, strict focused/full OpenSpec, status, doctor, scope/residue, dependency/public-contract, and exact hosted CI gates on one clean immutable source exact.
- [x] 4.3 Obtain one fresh cumulative Inspector review of the exact, covering the 36-site classification plus Recovery no-op listener, original-Error ownership, exact `toast.error(error.message)` versus `console.error(error)` user-impact routing, no lower-layer Toast coupling, non-error and manual-World exceptions, primary/cleanup ordering, test mutation sensitivity, and E2E cleanup evidence.
- [x] 4.4 Keep the pull request Draft and publish exact/tree/sole-parent/local-remote-PR identity plus terminal CI and review evidence; do not mark Ready, merge, deploy, release, or publish without later explicit authority.
