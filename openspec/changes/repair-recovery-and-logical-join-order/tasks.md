## 1. Authority And Baseline

- [ ] 1.1 Freeze current authority and same-PR parent `5cc4e597abf1f9c540b81bf5738fd0f7d1e7a86d`, historical source baseline `2f60913259f9ce834ffdf75f63eef87c9563e644`, inherited Refresh history from `a602149522c7038f29e13307bb925a48ed3848d7`, current protocol bytes/namespaces, Session observation classification, Artico peer lifecycle, supersession flow, Runtime readiness feedback/control state, ClientLease pending-RPC and stale-watchdog recovery, page connectivity observations, and current tab/page/session identity behavior before source edits.
- [ ] 1.2 Create one minimal source replacement only as a clean detached sole child of this docs exact, then advance only the existing `fix/cross-browser-action-lifecycle` / Draft PR #76 route; keep the Owner checkout, untracked `.pnpm-store/`, unrelated refs/PRs, CI, `master`, tags, and release metadata untouched.
- [ ] 1.3 Treat a602 Reviewer/QA facts, task #253, and invalid non-ancestor `9beec650...` bytes/evidence as non-transferable history. Do not cherry-pick that line. No intermediate implementation head may receive final gate or publication authority.

## 2. Protocol V3 And Logical Join Time

- [ ] 2.1 Add required strict safe non-negative `joinedAt` to `SessionMessage` only; keep `ChatSession`, SESSION_END, ChatMessage/history/reaction, World payload, codec algorithm, and limits unchanged.
- [ ] 2.2 Project only the Session-persisted local logical-generation time into every SESSION and bind the first accepted remote `presenceId` to its `user.id` and `joinedAt`. Fail closed on missing/invalid time without `clock.now()`, discovery, or baseline fallback; accept same-binding `name`/`avatar` projection refresh idempotently and drop a `user.id`/`joinedAt` mutation without changing membership or notices.
- [ ] 2.3 Reuse `{presenceId,joinedAt}` across Refresh, reconnect, recovery, duplicate publication, additional physical session, reattach, and supported host replacement; allocate a later time only for a true later generation.
- [ ] 2.4 Classify remote join eligibility by strict `remote.joinedAt > local.joinedAt` plus zero-to-one user transition. Make delayed historical discovery snapshot-only and keep any provisional later-join candidate attempt-owned until commit.
- [ ] 2.5 Move both Chat and World namespace inputs from exact v2 values to exact v3 values. Join only v3 rooms; add no optional field, v2 parser, dual room/send, translator, fallback, or compatibility alias.
- [ ] 2.6 Update public protocol declarations/schemas/parsers/golden fixtures and concise trust-boundary comments. World canonical payload bytes remain unchanged apart from selecting the v3 room.

## 3. Artico Demand-Driven Recovery

- [ ] 3.1 Introduce one adapter-private peer generation/restart owner shared by close-driven and join-driven recovery; preserve the host-lifetime peer id and private RoomTransport boundary.
- [ ] 3.2 When fresh desired room demand finds the retained peer `disconnected`, ensure exactly one replacement before pending joins wait for readiness. Concurrent Chat/World joins and repeated demand SHALL share it.
- [ ] 3.3 Fence every old peer open/error/close callback and delayed restart timer so it cannot join current rooms, settle current pending work, or replace a newer peer.
- [ ] 3.4 Preserve room-local leave semantics; cancel restart/timer work on dispose and settle every owned pending join exactly once without adding an unbounded retry loop or connecting watchdog.

## 4. Internal Supersession Cancellation

- [ ] 4.1 Replace the ordinary message-only supersession failure with one machine-classified internal cancellation across Runtime operation settlement; keep newest-wins generation and provisional Session/World aborts.
- [ ] 4.2 Make initial join, identity refresh, host recovery, and manual Refresh settle their own cancelled work without `Room.OnErrorEvent`, generic error Toast, false success, stale input retention, or stuck join/request/button loading.
- [ ] 4.3 Ensure only the winning attempt can commit/publish/retain current identity, same-id `name`/`avatar` projection, and outcome across every attached same-domain page; stale completion cannot clear or overwrite the winner.
- [ ] 4.4 Preserve existing user feedback for every genuine provider, protocol, persistence, Runtime, and join failure. Do not identify cancellation through message-string matching or add a second error/pending/Toast owner.

## 5. Fragment-Insensitive Page Routing

- [ ] 5.1 Define one canonical document-navigation identity that excludes only URL fragment while retaining scheme, host, port, path, and query; derive routing only from extension sender context validated against the background Tabs API registry.
- [ ] 5.2 Route provider responses to the exact trusted tab without using origin-wide `tabs.query()` delivery for a fragment-bearing href, and keep Offscreen relay equality fragment-insensitive while preserving wrong-tab, recycled-id, real-navigation, namespace, direction, and source rejection. Tabs inventory MAY reconstruct ownership but SHALL not broadcast responses.
- [ ] 5.3 Prove direct startup with `#fragment`, mounted `hashchange`, and hash change during the initial coordinator/Runtime handshake each mount or retain exactly one control/tab binding/document generation/logical presence with zero join/leave.
- [ ] 5.4 Preserve existing content bootstrap structure: no pre-App fallback UI, no origin-wide broadcast, no fragment-specific reconnect/remount, and no browser-specific business branch.

## 6. Bounded ClientLease Recovery And Feedback

- [ ] 6.1 Bind one overall 15,000ms deadline to each current logical ClientLease startup/recovery operation and one hard deadline no greater than 5,000ms or the remaining valid budget to every register/attach attempt; a pending call SHALL reject locally and cannot extend the current operation or settle a newer one.
- [ ] 6.2 Single-flight overlapping watchdog, generation, host-id, page-attachment, Port-loss, response-loss, reactivation, and retry/handoff recovery signals. A repeated signal SHALL join or validly hand off the current connectivity owner without parallel register/attach sequences, deadline reset, feedback restart, tab-owner removal, domain grace, or SESSION_END. A passive check suspended past its captured deadline SHALL lose settlement authority before current reconciliation/attachment settles.
- [ ] 6.3 Retire correlation and cancel request ownership for every expired, page-detached, aborted, stale-deadline, or superseded RPC/check/generation. Fence late resolve/reject work so it cannot publish HostPhase or readiness, replace snapshots, start watchdogs, settle a current task, publish/dismiss/replace connection feedback, release the current physical tab owner, or unregister the winning logical lease.
- [ ] 6.4 Delete `Toast.OnRoomSelfJoinRoomEffect` and its independent `SelfJoinRoomEvent -> LoadingCommand("Connected to the chat.")` random-owner producer; also delete the independent manual-Refresh `Reconnecting to the chat...` producer. Do not rename, reuse, hide, or remap either. Preserve immediate Runtime readiness truth and make fixed `AppFeedback` owner `webchat-runtime-readiness` the sole connection loading and terminal-error owner: exact loading copy `Connected to the chat.` for every manual Refresh or actual connect/join/reattach/host rebuild/recovery operation, including healthy retained-Runtime attachment, and exact logically final error copy `Connection failed` with no trailing period.
- [ ] 6.5 Passive polling or a health probe before promotion SHALL publish no loading owner. If it promotes, activate the sole readiness owner at that transition without resetting the current logical operation's valid budget. Dismiss only it on ready. Replace it with exact `Connection failed` only when the logical operation truly terminates with no current automatic continuation or handoff capable of succeeding; intermediate RPC/probe/Port/host failures, old deadlines, stale generations, Refresh, reactivation, retries, and handoffs SHALL publish no terminal frontend error.
- [ ] 6.6 Keep the sole readiness owner's Toast `loading` interval and the existing mounted Refresh control strictly aligned across direct/automatic connection or join, reattachment, host rebuild, recovery, manual Refresh, and the accepted 300ms minimum dwell: loading means disabled plus rotating; success dismisses that owner without `Ready to chat` or another success Toast, while only logical final failure replaces it with `Connection failed` and ends control loading. A pure poll/probe SHALL create neither projection nor change ordinary eligibility; promotion into actual recovery SHALL begin both once. Recompute ordinary retry eligibility, preserve unrelated entries and the original Toaster structure/visuals, fence intermediate/stale settlement from current control/feedback state, and add no pre-App control or second connection owner.

## 7. Tabs API-Owned Physical Tab Lifecycle

- [ ] 7.1 Make the background coordinator own one current host-to-tabs registry: one host MAY own many eligible tabs, while each live `tabId` occurs at most once. Keep Runtime Lifecycle as the sole domain lease/grace owner; do not add a parallel background domain map.
- [ ] 7.2 Accept comctx tab metadata only after validation against extension-provided sender context and current Tabs API state. Keep trusted physical `tabId`, document generation, and logical `sessionId`/`presenceId`/`joinedAt` separate; reject stale host/document generations, recycled ids, wrong tabs, and payload-only claims.
- [ ] 7.3 Treat inactive, hidden, frozen, discarded, ping-missing, heartbeat-missing, and Port-disconnected states as connectivity facts only. They MAY enter bounded ClientLease recovery but SHALL NOT remove tab ownership, start last-tab grace, retire membership, or publish SESSION_END.
- [ ] 7.4 Use trusted tab removal, lost eligibility, or Runtime-domain change to release the old binding exactly once. Preserve tab and logical presence across hash changes; on reload or same-domain eligible document navigation, replace only document generation and idempotently reattach. A domain-B destination SHALL not inherit domain-A presence merely because `tabId` is unchanged.
- [ ] 7.5 Reconstruct the replacement host registry from current eligible tabs and trusted reattachments after supported background/Runtime replacement. Add parent fail-first and candidate controls for one host/multiple tabs, non-last versus last close, inactivity/freeze/discard, ping/Port loss, hash/reload/same-domain navigation, other-domain/ineligible navigation, host replacement, spoofed/reused tab id, and exactly-once domain release with no false join/leave or duplicate lease.

## 8. Combined Regression Matrix

- [ ] 8.1 Add explicit parent fail-firsts proving the legacy self-join random owner can coexist with Runtime readiness, manual Refresh independently publishes `Reconnecting to the chat...`, and an old suspended watchdog check can publish unavailable before the next check succeeds. Candidate pass-after SHALL prove both duplicate connection producers are absent; exactly one fixed owner exists with loading `Connected to the chat.` and logically final error `Connection failed`; passive polling creates no Toast/control loading; promotion starts the sole owner and mounted control once without deadline reset; Toast/mounted-control loading intervals match; manual/direct parity holds; every ultimately successful initial/manual/reactivation/reattach/recovery path has zero transient error; success dismisses only the owner without any success descriptor; only logical final failure creates the same-owner terminal-error retry state; and no-leave retry, joined reconnect preservation, request single-flight, cleanup, and stale fences hold. Preserve the existing Header regression contract that online groups are stably ordered by descending `users.length`, preserving source order for equal counts without moving sorting into Runtime or World transport.
- [ ] 8.2 Add Artico lifecycle matrix coverage for empty/non-empty demand, ready/connecting/disconnected peer state, concurrent Chat/World joins, exactly one replacement, old callback/timer isolation, leave, and dispose.
- [ ] 8.3 Add same-domain supersession races for two-page avatar update, avatar/manual Refresh overlap, avatar/host recovery overlap, winner success/failure ordering, same-id `name`/`avatar` projection convergence and idempotence, stale superseded-attempt projection fencing, zero lifecycle notices/cancellation Toast, and genuine-error controls.
- [ ] 8.4 Add an explicit parent fail-first for both-late A discovery plus historical SESSION after B commit, then candidate pass-after across discovery/SESSION before, split across, and both after commit. Prove exact notice projections A `[A joined, B joined]`, B `[B joined]`, converged membership, and no duplicate/reconnect/reload/host-replacement notice.
- [ ] 8.5 Add later-during-provisional, equal/older timestamp, missing/invalid time with no receiver-local fallback, mutated same-presence `user.id`/time rejection, true later return, exact v3/v3 exchange, v1/v2/v3 namespace isolation, and SystemNotice local-only/no-wire/no-history controls.
- [ ] 8.6 Add a parent fail-first proving a direct `https://www.v2ex.com/t/1230408#reply6`-shape URL does not mount, then candidate fragment routing controls for direct hash, mounted hashchange, in-flight hash change, multiple same-origin tabs, changed path/query navigation, recycled tab id, and forged/stale provider response. This existing cumulative requirement is mandatory and cannot be omitted because newer smoke regressions were routed later.
- [ ] 8.7 Add deterministic ClientLease fail-before/pass-after controls for passive healthy/error polling with no readiness/control loading, poll-to-recovery promotion with the sole owner and no deadline reset, healthy retained-host refresh with active then dismissed `Connected to the chat.`, direct/automatic connection button disable/rotation without a click, forever-pending and rejected register/attach, host replacement, Port/response loss, two overlapping recovery signals, late completion after timeout/page-detach/supersession, connectivity-only loss with retained tab ownership, and logical-terminal readiness/control settlement. Required stale-deadline control: suspend a passive check beyond its captured deadline, resume/reactivate, let the current attach succeed, preserve tab binding/logical presence, and prove the old completion cannot publish HostPhase/readiness unavailable or frontend `Connection failed`.
- [ ] 8.8 Run focused and full tests, type/format/lint checks, strict OpenSpec validation, production Chrome MV3/Firefox MV2 builds, and only the exact-bound focused browser flows routed by Planner. Do not expand this repair into the full release matrix unless a failure requires it.
- [ ] 8.9 Prove direct and cumulative scope: no public ChatRoom expansion, MessageStore/database change, unrelated UI surface or panel-structure change, Toast renderer/visual change, alternate bootstrap UI, World wire-payload change, provider retry/watchdog, custom background keepalive/wake fallback, dependency/workflow/WXT change, release-version edit, compatibility path, origin-wide response broadcast, parallel domain lease/grace owner, or v2 active namespace residue. This refinement's presentation source change is limited to deleting the legacy self-join and independent manual-Refresh connection producers, applying the fixed owner's exact loading/terminal copies and logical-terminal settlement rule, and preserving the required Refresh control projection; lifecycle source change is limited to the necessary stale-watchdog/deadline/generation settlement fence. The existing Header stable online-count ordering regression remains unchanged.

## 9. Review And Release

- [ ] 9.1 Freeze one clean cumulative immutable replacement source exact as the sole child of this docs authority, with exact/tree/parent/patch identities, direct/cumulative path inventory, zero unintended refs, and exact-bound evidence; prior `2f60913259f9ce834ffdf75f63eef87c9563e644` verdicts and docs-parent `5cc4e597...` evidence do not transfer to the source candidate.
- [ ] 9.2 Route fresh Reviewer and QA in parallel only after the final exact exists. Both SHALL review/test the complete seven-repair matrix; prior a602 or partial-head verdicts do not transfer.
- [ ] 9.3 After both final PASS, synchronize only that exact to the Owner checkout without touching `.pnpm-store/`. Owner verifies seven scenarios in one acceptance: passive polling shows no readiness/control loading while failed-join manual Refresh and direct/automatic connection operations show exactly one `Connected to the chat.` readiness Toast and disable/rotate Refresh until that owner terminates, with no `Reconnecting to the chat...`, polling promotion starting once without deadline reset, every ultimately successful initial/manual/reactivation/reattach/recovery path dismissing without transient error or success Toast, and only logical final failure showing exact `Connection failed`; disconnected-peer retry; multi-page avatar update without supersession Toast; exact A-before-B notice projections; direct fragment-URL startup; retained-Runtime refresh/reactivation with bounded loading and zero stale-deadline error flash; and one host with multiple tabs where inactivity/connectivity loss preserves presence while trusted close or eligibility/domain exit releases exactly once.
- [ ] 9.4 Publish only after Owner acceptance by verified normal fast-forward and stop on remote drift. Do not modify `master`, tags, package/app version, release metadata, or release notes in this change.
