# show-danmaku-only-in-visible-document

Push each new Danmaku only when its existing setting is enabled and the local content document is visible, without
changing already accepted Danmaku when document visibility changes.
