## 1. Authority And Evidence Reset

- [x] 1.1 Bind this superseding docs authority to package-first parent `74d0c67eaf24f68bf731c8c9205cea7aa6c6792c` (tree `6fd9693794491d5ec6c59084925ec8a2d78bd521`) and verify a clean detached/ref-free starting worktree. Round 18 `3ae2b81ebaf31dfd368affabdd387fe204929420` and every blocked candidate are neither parents nor transferable evidence.
- [x] 1.2 Record QA task #309 as a Chrome executable verification-control blocker, not a product/package/helper verdict. Its automated, static, Review, Chrome, cleanup, and partial results do not transfer, and Firefox was not reached.
- [x] 1.3 Record task #310's read-only causal result: the temporary adapter hardcoded branded Chrome 150 instead of receiving the repository canonical's explicit side-load-capable Chrome for Testing/Chromium executable.
- [x] 1.4 Record the preserved facts: package MV3 declares `background.service_worker = background.js`; the artifact contains that entry; CDP discovery classified the foreign Google Network Speech worker; no WebChat target/session appeared; and no helper timing/source repair is supported.
- [x] 1.5 Freeze the direct tracked implementation whitelist: new `e2e/chrome-native-action-lifecycle.ts`, new `e2e/chrome-native-action-lifecycle.test.ts`, and checkbox-only updates to this file. Treat every other tracked path as protected.
- [x] 1.6 Freeze the two source byte identities from Round 18: helper SHA-256 `6763e476722dd7b7675820b753806ea5908e13560eeef328a15d6ce4c27ff160` and focused-test SHA-256 `9f4a27172bf0f60446dd5f1c5776d14df95dc4c295e1c49c0e9d019eb7f2baa5`. Any byte change requires new authority.
- [x] 1.7 Freeze the real adapter as a test-owned, SHA-256-recorded QA artifact outside tracked repository scope. It may change only executable injection, validation, privacy-safe identity evidence, and precondition controls; it may not change helper/product/package/lifecycle semantics.

## 2. Explicit Chrome Executable Precondition

- [ ] 2.1 Require one caller-injected absolute executable path. Reject a missing or relative value, branded application hardcode, default, `PATH` lookup, application scan, browser discovery, fallback, or Owner-browser reuse.
- [ ] 2.2 Before any persistent profile, CDP browser, helper budget, or helper call, use one non-resetting maximum 10-second precondition to resolve `realpath`, require a regular executable with execute access, hash the requested path/canonical path/executable bytes, and run exactly one bounded `--version` probe.
- [ ] 2.3 Accept only an exact `Google Chrome for Testing <version>` or `Chromium <version>` identity with a non-empty version. Reject branded Google Chrome, Microsoft Edge, derivatives, unknown/ambiguous labels, symlink-to-rejected targets, non-zero probe exit, timeout, output over 512 bytes, or probe/launch canonical-path mismatch.
- [ ] 2.4 Record only product family, parsed version, requested-path SHA-256, canonical-path SHA-256, executable-byte SHA-256, bounded version-output SHA-256, and one derived privacy-safe executable identity token. Keep raw absolute paths transient and out of reports/helper evidence.
- [ ] 2.5 Emit distinct `chrome-executable-precondition-failed` evidence before lifecycle on any invalid or unresolved identity. Prove the bounded probe is cleaned and no persistent profile, CDP, worker discovery, accepted target, helper timeline, or native action began; do not map it to a helper outcome.
- [ ] 2.6 Cover accepted Chrome for Testing and Chromium plus missing, relative, nonexistent, non-executable, branded Chrome, branded Edge, derivative, unknown, ambiguous, symlink-to-rejected, non-zero, timeout, output-overflow, and probe/launch-mismatch controls without launching canonical lifecycle.
- [ ] 2.7 After PASS, launch exactly the validated canonical executable with the exact package, isolated profile, and only `about:blank`, pass the privacy-safe identity token into the unchanged helper context, and provide no alternate executable or retry.

## 3. Deterministic Lifecycle Diagnostic

- [ ] 3.1 Recreate the dependency-free helper and focused test byte-identically to the recorded Round 18 SHA-256 values. Do not add executable logic, a package script, existing-runner edit, reporter, aggregator, or general cleanup owner.
- [ ] 3.2 Require `about:blank` startup; establish target discovery, flattened auto-attach, per-session Runtime/Log/Page observation, and the event sink before accepted-target creation.
- [ ] 3.3 Parse the packaged manifest first and require MV3 plus a non-empty `background.service_worker`. Do not accept a caller-supplied extension ID or a first/global-only worker shortcut.
- [ ] 3.4 Use key-order-only canonical JSON and one non-resetting maximum 30-second pre-target discovery deadline. Classify every current worker before deciding uniqueness; derive the extension ID only from one URL-host/Runtime-ID, exact-entry, canonical-manifest match.
- [ ] 3.5 Record bounded privacy-safe per-worker order/time, target URL/session, Runtime ID, allowlisted manifest projection, packaged/Runtime SHA-256 canonical digests, and capped sorted JSON Pointer diff evidence. A diff-overflow marker remains non-equal by digest; fail on total worker-record overflow or unresolved classification.
- [ ] 3.6 Treat zero exact candidates at the deadline or more than one at a decision fence as `extension-setup-failed`. Keep classified foreign workers as evidence only; never let them supply identity or replace the exact binding.
- [ ] 3.7 Bind the exact worker target/session/ID/entry/manifest tuple immutably. Classify later workers only within the remaining current budget; reject unresolved, exact duplicate/replacement, or tuple change while allowing a fully classified unrelated worker to remain evidence only.
- [ ] 3.8 Issue exactly one `Target.createTarget({ url: 'https://example.com/' })`; bind its target/session/main frame; reject startup accepted pages, second targets, replacement, `Page.navigate`, reload, refresh, retry, and repair.
- [ ] 3.9 Use one non-resetting maximum 30-second accepted-target lifecycle budget and one exact binding across navigation, isolated context, logs, DOM, terminal outcome, and action authorization.
- [ ] 3.10 Record a bounded ordered timeline for worker discovery/continuity, target/frame lifecycle, page-bound execution contexts, normalized console/exception events, structural DOM samples, and an unconditional final state. Capture no arbitrary DOM, manifest raw values outside the safe projection, cookies, storage, credentials, or user data.
- [ ] 3.11 Emit exactly one fail-closed helper outcome: `extension-setup-failed`, `target-lifecycle-failed`, `content-context-absent`, `shared-runtime-unavailable`, `content-mount-absent`, `unexpected-content-failure`, or `mounted`.
- [ ] 3.12 Return action authorization only for clean `mounted` with the same worker and page binding; never click, dispatch an action event, call the product command, open options, or repair a failed lifecycle from the helper.

## 4. Focused Controls And Protected Scope

- [ ] 4.1 Cover invalid packaged MV3/background entry, foreign-first/exact-late, only-foreign, duplicate exact, unresolved candidate, entry mismatch, Runtime-ID mismatch, canonical manifest mismatch, accepted object-key reorder, and rejected array/value/field/default/alias/path differences.
- [ ] 4.2 Cover a fully classified unrelated worker after binding, exact duplicate/replacement, and every bound worker target/session/ID/entry/manifest continuity change.
- [ ] 4.3 Cover correct observation-before-target chronology and reject an accepted startup page, late listeners, a second target request, target replacement, wrong session/frame, foreign context, deadline reset, retry, reload, and repair.
- [ ] 4.4 Cover bounded diff-overflow evidence, total worker/event overflow, worker/options/main-world contexts, `Shared runtime unavailable`, another extension error, isolated-context-without-mount, false host-page root, missing final evidence, and clean mount authorization.
- [ ] 4.5 Prove the helper never invokes native action and that every non-mounted or invalidated-binding branch withholds the caller's click callback before activation.
- [ ] 4.6 Prove both tracked source files match the recorded SHA-256 values and every existing E2E file, including Chrome harness/runtime and both Firefox precondition files, remains byte-identical.
- [ ] 4.7 Prove `src/**`, WXT/manifest/permissions, dependencies/lockfile, package scripts, timeouts, Runtime/coordinator/Offscreen/protocol/storage/UI, workflows/CI, reports, release metadata, refs/remotes, and Owner checkout remain unchanged.
- [ ] 4.8 Run implementation-owned adapter controls, focused/full tests, format/lint/type, strict OpenSpec, production Chrome MV3 and Firefox MV2 build/package/manifest, scope, and lineage gates. Do not run or claim canonical browser QA from the implementation seat.
- [ ] 4.9 Freeze one clean detached/ref-free immutable implementation exact as the sole child of this docs exact and freeze the test-owned real-adapter source SHA-256. Do not push, update PR/CI, merge, release, or touch the Owner checkout.

## 5. Fresh Review And Full Cross-Browser QA

- [ ] 5.1 Obtain one fresh Reviewer PASS for executable-precondition authority and controls, adapter artifact identity, the two tracked source digests, package-first identity authority, strict canonical relation, worker evidence/cardinality/continuity, chronology, target/context binding, terminal precedence, action withholding, direct scope, and protected surfaces.
- [ ] 5.2 After Review PASS, obtain one fresh same-seat QA verdict from zero for the complete real Chrome MV3 plus Firefox MV2 matrix. Do not reuse QA task #287, QA task #298, QA task #309, or any earlier candidate evidence.
- [ ] 5.3 In Chrome, record the explicit accepted CfT/Chromium identity and adapter SHA-256, pass executable precondition before lifecycle, start only `about:blank`, preserve every worker record/diff, derive one exact worker/manifest binding, create the accepted target exactly once, preserve the full timeline, and reach clean `mounted` without executable fallback, retry, reload, target substitution, or unexpected error.
- [ ] 5.4 After Chrome `mounted` authorization, perform exactly one real native toolbar action, prove `afterOptionsCount - beforeOptionsCount = 1` plus pre/post-action content Runtime on the same accepted target, and reject event dispatch, direct product invocation, manual options navigation, or post-click repair.
- [ ] 5.5 In Firefox, rerun initial startup plus two same-profile owned-process restarts and satisfy every existing exact add-on, Background Page, physical identity, one native click, options `+1`, pre/post Runtime, unexpected-error, and no-repair requirement in every generation.
- [ ] 5.6 Prove zero residual executable probes, owned Chrome/Firefox browsers, drivers, profiles, packages, processes, ports, sessions, listeners, tabs, and temporary resources without touching unrelated Owner resources.

## 6. Follow-Up Routing

- [ ] 6.1 If executable precondition fails, stop as a harness setup result; do not invoke the helper, select another executable, retry task #309, or edit product/package/helper source.
- [ ] 6.2 If an accepted explicit executable still reaches `extension-setup-failed` because no exact worker appears, route a separate activation/observation authority from that fresh artifact; do not reinterpret the branded-Chrome run.
- [ ] 6.3 If the helper ends `content-context-absent`, `shared-runtime-unavailable`, `content-mount-absent`, or `unexpected-content-failure`, route the corresponding separate product/browser-lifecycle authority without broadening this change.
- [ ] 6.4 If the full matrix passes, retain exact-bound artifacts for later release coordination, but do not push, mutate PR/CI, merge, release, or update the Owner checkout without separate explicit authorization.
